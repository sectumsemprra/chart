# Core ML
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.12.0
datasets>=2.18.0

# DeepSpeed (optional, for multi-GPU)
deepspeed>=0.14.0

# Vision
qwen-vl-utils>=0.0.8
Pillow>=10.0.0

# Sentence Similarity
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Utilities
wandb>=0.16.0
tqdm>=4.66.0
numpy>=1.24.0

# Optional: Flash Attention (requires CUDA)
# flash-attn>=2.5.0
